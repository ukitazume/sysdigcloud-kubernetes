apiVersion: apps/v1beta1
kind: StatefulSet
metadata:
  name: sysdigcloud-cassandra
spec:
  template:
    spec:
      containers:
        - name: cassandra  
          resources:
            limits:
              cpu: "8"
              memory: 8Gi
            requests:
              cpu: "4"
              memory: 2Gi      
