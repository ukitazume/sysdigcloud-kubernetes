apiVersion: apps/v1beta1
kind: StatefulSet
metadata:
  name: redis-primary
spec:
  template:
    spec:
      containers:
        - name: redis-primary
          resources:
            limits:
              cpu: "1"
              memory: 8Gi
            requests:
              cpu: "200m"
              memory: 2Gi
