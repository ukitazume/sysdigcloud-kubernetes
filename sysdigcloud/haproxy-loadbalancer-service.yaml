apiVersion: v1
kind: Service
metadata:
  name: sysdigcloud-haproxy
  labels:
    app: sysdigcloud
    role: haproxy
spec:
  type: LoadBalancer
  ports:
    - port: 443
      name: https
  selector:
    app: sysdigcloud
    role: haproxy
