apiVersion: v1
kind: Service
metadata:
  name: {{ .sysdigCassandraServiceName }}
  namespace: {{ .sysdigNamespace }}
  labels:
    app: sysdigcloud
    role: cassandra
spec:
  clusterIP: None
  ports:
    - port: {{ .sysdigCassandraCqlPort }}
      name: cql
    - port: {{ .sysdigCassandraThriftPort }}
      name: thrift
    - port: {{ .sysdigCassandraIntraNodePort }}
      name: intra-node
    - port: {{ .sysdigCassandraIntraNodeTlsPort }}
      name: tls-intra-node
    - port: {{ .sysdigCassandraJmxPort }}
      name: jmx
  selector:
    app: sysdigcloud
    role: cassandra
---
apiVersion: "apps/v1beta1"
kind: StatefulSet
metadata:
  name: sdc-cassandra
  namespace: {{ .sysdigNamespace }}
  labels:
    app: sysdigcloud
    role: cassandra
spec:
  serviceName: {{ .sysdigCassandraServiceName }}
  replicas: {{ .sysdigCassandraReplicaCount }}
  selector:
    matchLabels:
      app: sysdigcloud
      role: cassandra    
  template:
    metadata:
      labels:
        app: sysdigcloud
        role: cassandra
    spec:
      containers:
      - name: cassandra
        image: {{ .sysdigCassandraImage }}
        ports:
          - containerPort: {{ .sysdigCassandraCqlPort }}
            name: cql
          - containerPort: {{ .sysdigCassandraThriftPort }}
            name: thrift
          - containerPort: {{ .sysdigCassandraIntraNodePort }}
            name: intra-node
          - containerPort: {{ .sysdigCassandraIntraNodeTlsPort }}
            name: tls-intra-node
          - containerPort: {{ .sysdigCassandraJmxPort }}
            name: jmx
        resources:
          requests:
           cpu: {{ .cassandraResourceRequestCpu }}
           memory: {{ .cassandraResourceRequestMemory }}
          limits:
           cpu: {{ .cassandraResourceLimitsCpu }}
           memory: {{ .cassandraResourceLimitsMemory }}
        securityContext:
          capabilities:
            add:
              - IPC_LOCK
        lifecycle:
          preStop:
            exec:
              command: ["/bin/sh", "-c", "nodetool drain"]
        readinessProbe:
          exec:
            command:
            - /bin/bash
            - -c
            - nodetool status | egrep $(hostname -i) | egrep UN 
          initialDelaySeconds: 15
          timeoutSeconds: 5            
        env:
          - name: CASSANDRA_SEEDS
            valueFrom:
                configMapKeyRef:
                  name: sysdigcloud-config
                  key: cassandra.seeds
         # - name: CASSANDRA_SEED_PROVIDER
         #   valueFrom:
         #     configMapKeyRef:
         #         name: sysdigcloud-config
         #         key: cassandra.seed.provider          
          - name: CASSANDRA_SERVICE
            valueFrom:
                configMapKeyRef:
                  name: sysdigcloud-config
                  key: cassandra.service
          - name: CASSANDRA_CLUSTER_NAME
            valueFrom:
                configMapKeyRef:
                  name: sysdigcloud-config
                  key: cassandra.cluster.name
          - name: CASSANDRA_ENDPOINT_SNITCH
            valueFrom:
                configMapKeyRef:
                  name: sysdigcloud-config
                  key: cassandra.endpoint.snitch
          - name: CASSANDRA_START_RPC
            valueFrom:
                configMapKeyRef:
                  name: sysdigcloud-config
                  key: cassandra.start.rpc                                                                                
          - name: JVM_EXTRA_OPTS
            valueFrom:
                configMapKeyRef:
                  name: sysdigcloud-config
                  key: cassandra.jvm.options
          - name: CASSANDRA_SECURE
            valueFrom:
                configMapKeyRef:
                  name: sysdigcloud-config
                  key: cassandra.secure
          - name: CASSANDRA_SSL
            valueFrom:
                configMapKeyRef:
                  name: sysdigcloud-config
                  key: cassandra.ssl.enabled
          - name: POD_IP
            valueFrom:
              fieldRef:
                fieldPath: status.podIP
          - name: POD_NAMESPACE
            valueFrom:
              fieldRef:
                fieldPath: metadata.namespace
        volumeMounts:
        - name: data
          mountPath: /var/lib/cassandra
      imagePullSecrets:
      - name: sysdigcloud-pull-secret
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: {{ .sysdigCassandraDiskSize }}
      storageClassName: {{ .sysdigStorageclassName }}
