apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: sysdigcloud-worker  
spec:
  template:
    spec:
      containers:
        - name: worker        
          resources:
            limits:
              cpu: "8"
              memory: "8Gi"
            requests:
              cpu: "2"
              memory: "2Gi"
