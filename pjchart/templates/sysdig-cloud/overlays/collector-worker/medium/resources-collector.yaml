apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: sysdigcloud-collector  
spec:
  template:
    spec:
      containers:
        - name: collector        
          resources:
            limits:
              cpu: "8"
              memory: "8Gi"
            requests:
              cpu: "2"
              memory: "2Gi"
