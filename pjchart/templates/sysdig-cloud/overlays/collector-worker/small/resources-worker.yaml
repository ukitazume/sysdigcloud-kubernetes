apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: sysdigcloud-worker  
spec:
  template:
    spec:
      containers:
        - name: worker        
          resources:
            limits:
              cpu: "4"
              memory: "4Gi"
            requests:
              cpu: "1"
              memory: "1Gi"
