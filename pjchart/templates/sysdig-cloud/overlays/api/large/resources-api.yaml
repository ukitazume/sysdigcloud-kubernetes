apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: sysdigcloud-api  
spec:
  template:
    spec:
      containers:
        - name: api        
          resources:
            limits:
              cpu: "8"
              memory: "16Gi"
            requests:
              cpu: "4"
              memory: "4Gi"
